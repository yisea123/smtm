1.范例运行时，请加入 pci9014.h ，pos_latch.cpp，pos_latch.h

2.范例包含两个功能，一个是0轴的位置比较，一个是在位置比较成功时，锁存1轴位置。

  比较触发的来源可以选择命令寄存器或者脉冲反馈寄存器，锁存的来源只能是脉冲反馈寄存器。如果没有接编码器反馈的话，运行位置锁存的时候会报失败

3.对应的触发点是 PCI9014 CN1上的52脚  DO4(从0开始数)，是集电极开路输出

4.接线时，如果相机端触发信号串 4.7K 以上电阻，则可以 提供 24V电压，如果只是几百K，请提供 5V电压。